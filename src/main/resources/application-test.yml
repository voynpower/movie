spring:
  datasource:
    url: jdbc:h2:mem:testdb   # In-memory H2 database
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true  # H2 konsolini yoqish
